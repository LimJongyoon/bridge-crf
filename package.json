{
  "name": "bridge-crf",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "cd frontend && npm run build && npm run export",
    "pack": "electron-packager . bridge-crf --platform=win32 --arch=x64 --overwrite",
    "electron-dev": "cd frontend && npm run electron-dev"
  },
  "devDependencies": {
    "electron": "^29.4.6",
    "electron-packager": "^17.1.2"
  }
}